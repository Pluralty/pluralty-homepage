# Public API endpoint for the contact form (NOT a secret — it is the public
# API Gateway invoke URL produced by Terraform output `contact_api_endpoint`).
# Set this at build time. Locally you can leave it pointing at the deployed API.
PUBLIC_CONTACT_API_URL=https://example.execute-api.us-east-1.amazonaws.com/contact
