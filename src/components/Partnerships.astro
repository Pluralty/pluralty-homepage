---
import Tr from './Tr.astro';
import awsLogo from '../assets/Amazon_Web_Services_Logo.svg';
import tdsynnexLogo from '../assets/tdsynnex-logo.jpeg';

const items = [
  {
    k: 'tdsynnex',
    color: '#005B9A',
    bg: 'rgba(0,91,154,0.15)',
    img: tdsynnexLogo,
    alt: 'TD SYNNEX',
  },
  {
    k: 'aws',
    color: '#FF9900',
    bg: 'rgba(255,153,0,0.12)',
    img: awsLogo,
    alt: 'AWS',
  },
  {
    k: 'cert',
    abbr: '✓\nCERT',
    color: '#7B2FB5',
    bg: 'rgba(123,47,181,0.15)',
  },
];
---

<section class="section" id="partners">
  <div class="wrap">
    <header class="head" data-reveal>
      <span class="eyebrow"><Tr k="partners.eyebrow" /></span>
      <h2><Tr k="partners.title" as="span" /></h2>
    </header>
    <div class="grid">
      {
        items.map((it, i) => (
          <article class="trust-card" data-reveal style={`--reveal-delay:${i * 0.06}s`}>
            <div class="logo-area" style={`--lc:${it.color};--lb:${it.bg}`}>
              {it.img ? (
                <img src={it.img.src} alt={it.alt} class="partner-logo" loading="lazy" decoding="async" />
              ) : (
                <span class="abbr">{it.abbr}</span>
              )}
            </div>
            <h3>
              <Tr k={`partners.${it.k}.t`} />
            </h3>
            <p>
              <Tr k={`partners.${it.k}.d`} as="span" />
            </p>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .head {
    max-width: 44rem;
    margin-bottom: var(--sp-7);
  }
  .head h2 {
    font-size: var(--step-3);
    margin-top: var(--sp-3);
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-4);
  }
  .trust-card {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--r-card);
    padding: var(--sp-6);
    display: flex;
    flex-direction: column;
    gap: var(--sp-4);
    transition: border-color 0.3s, transform var(--dur) var(--ease);
  }
  .trust-card:hover {
    border-color: var(--line-strong);
    transform: translateY(-2px);
  }
  .logo-area {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 72px;
    height: 52px;
    border-radius: 10px;
    background: var(--lb, rgba(75,0,118,0.15));
    border: 1px solid color-mix(in srgb, var(--lc, var(--brand)) 30%, transparent);
  }
  .logo-area :global(svg) {
    width: 52px;
    height: auto;
  }
  .partner-logo {
    max-width: 52px;
    max-height: 36px;
    width: auto;
    height: auto;
    object-fit: contain;
  }
  .abbr {
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: 11px;
    letter-spacing: 0.05em;
    color: var(--lc, var(--brand-glow));
    white-space: pre;
    text-align: center;
    line-height: 1.3;
  }
  .trust-card h3 {
    font-size: var(--step-1);
  }
  .trust-card p {
    color: var(--muted);
    font-size: var(--step-0);
    flex: 1;
  }
  @media (max-width: 820px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
